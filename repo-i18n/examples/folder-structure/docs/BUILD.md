# Build Guide

This file is **hand-maintained** (not generated by CI).

## Prerequisites

- Your language / framework SDK
- Platform toolchains as required (e.g. Android SDK, Windows, Linux)

## Build

```bash
# install dependencies
# then build per platform
```

See the CI workflow (`.github/workflows/i18n.yml`) for the exact steps used in
automated builds.
